[
  {
    "pattern": "^(([_A-Za-z0-9.-]+)|_)@[A-Za-z0-9](?:[A-Za-z0-9-]*[A-Za-z0-9])?(?:\\.[A-Za-z0-9](?:[A-Za-z0-9-]*[A-Za-z0-9])?)+$",
    "reason": "RFC 5322 email validation — too complex for a single native op"
  },
  {
    "pattern": "^nostr:((npub|note)1[02-9ac-hj-np-z]{58}|(nprofile|nevent|naddr)1[02-9ac-hj-np-z]+)$",
    "reason": "Nostr URI with multiple bech32 variants — needs compound bech32+prefix op"
  },
  {
    "pattern": "^\\d+x\\d+$",
    "reason": "Image dimensions (WxH) — needs digits_x_digits op"
  },
  {
    "pattern": "^[A-Za-z0-9_\\-+]+(?:\\/[A-Za-z0-9_\\-+]+)*$",
    "reason": "Slash-separated path segments — needs segment_path op"
  },
  {
    "pattern": "^[0-9bcdefghjkmnpqrstuvwxyz]+",
    "reason": "Geohash charset (no anchor) — needs chars_in variant without end anchor"
  },
  {
    "pattern": "^refs\\/(heads|tags)\\/[^\\s]+$",
    "reason": "Git ref path with alternation group — needs starts_with_any + no-whitespace tail"
  },
  {
    "pattern": "^[0-9]+(\\.[0-9]+)*$",
    "reason": "Dot-separated version number — needs semver/dotted_digits op"
  },
  {
    "pattern": "^\\S+( \\S+)*$",
    "reason": "Space-separated non-whitespace tokens — needs token_list op"
  },
  {
    "pattern": "^[A-Za-z0-9][A-Za-z0-9!#$&^_.+-]*/[A-Za-z0-9][A-Za-z0-9!#$&^_.+-]*$",
    "reason": "Simple MIME type (mixed-case variant) — needs mime_type variant with uppercase"
  },
  {
    "pattern": "^(02|03)[a-f0-9]{64}$",
    "reason": "Compressed secp256k1 public key — needs hex_prefixed variant with alternation"
  },
  {
    "pattern": "^[^A-Z]+$",
    "reason": "Negated character class (no uppercase) — chars_in only handles positive sets"
  },
  {
    "pattern": "^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$",
    "reason": "Base64 validation — complex repetition structure needs dedicated op"
  },
  {
    "pattern": "^[a-zA-Z0-9_-]+: .+",
    "reason": "Key-value header format (no end anchor) — needs prefix_pattern op"
  },
  {
    "pattern": "^[A-Za-z0-9+/]+={0,2}\\?iv=[A-Za-z0-9+/]+={0,2}$",
    "reason": "NIP-04 encrypted content (base64?iv=base64) — needs dedicated op"
  },
  {
    "pattern": "^refs/.*",
    "reason": "Git refs prefix (no end anchor) — needs starts_with variant without end anchor"
  },
  {
    "pattern": "^[0-9]+:.+",
    "reason": "Kind:remainder coordinate prefix (no end anchor) — needs prefix_pattern op"
  },
  {
    "pattern": "^https?://\\S+$",
    "reason": "HTTP URL with non-whitespace tail — needs starts_with_any + no-whitespace variant"
  },
  {
    "pattern": "^wss?://[a-zA-Z0-9.-]+(?::[0-9]+)?(?:/.*)?$",
    "reason": "Relay URL variant without underscore in hostname — relay_url op uses [._-] charset"
  },
  {
    "pattern": "^((ws://|wss://).+)?$",
    "reason": "Legacy optional relay URL — outer ()? wrapping not handled by relay_url classifier"
  }
]
